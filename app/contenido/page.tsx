import { BookOpen } from "lucide-react";
import { PlaceholderSection } from "@/components/PlaceholderSection";

export default function ContenidoPage() {
  return (
    <PlaceholderSection
      icon={BookOpen}
      title="Contenido"
      description="Microbiología, hematología, bioquímica, parasitología, inmunología y citología."
    />
  );
}
