import { GraduationCap } from "lucide-react";
import { PlaceholderSection } from "@/components/PlaceholderSection";

export default function EstudioPage() {
  return (
    <PlaceholderSection
      icon={GraduationCap}
      title="Estudio"
      description="Materiales y herramientas de estudio para el equipo del laboratorio."
    />
  );
}
